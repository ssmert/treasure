####################################################################################
#                                      SPRING                                      #
####################################################################################
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:mysql://localhost:3306/must_need_db?serverTimezone=UTC&useLegacyDatetimeCode=false&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: ENC(HF+hxWiCW8MoV8X1VKlPrW/r8LDE8PfS)
  sql:
    init:
      # always | embedded | never
      mode: never
      data-locations: classpath:data/test.sql
  jpa:
    hibernate:
      # create | create-drop | none | update | validate
      ddl-auto: create

####################################################################################
#                                    KEYCLOAK                                      #
####################################################################################
keycloak:
  auth-server-url: "http://localhost:5555/auth"
  realm: "mune"
  # client
  resource: "mune_client-care"
  # client secret
  credentials:
    secret: "OCCz96t9UL5ylMyjsCBDVoZVdJkYuaLO"

####################################################################################
#                                      CUSTOM                                      #
####################################################################################
custom:
  keycloak:
    username: admin
    password: qwert12345
    logout-url: ${keycloak.auth-server-url}/realms/${keycloak.realm}/protocol/openid-connect/logout